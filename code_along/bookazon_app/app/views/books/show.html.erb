<div class="col-xs-12">
  <div class="row-fluid">
    <h1> <%= @book.title %> </h1>
    <ul> <%= image_tag(@book.image_url, class: 'img-thumbnail img-responsive') %> </ul>
  </div>
  <div class ="row-fluid">
    <ul> <%= @book.author %> </ul>
    <ul><%= @book.year_published %> </ul>
    <ul><%= @book.genre %> </ul>
  </div>
</div>

<%= link_to 'edit', edit_book_path(@book) %> <br>
<%= link_to 'index', books_path %>
